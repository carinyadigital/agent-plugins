# delivery-lead — Managed Agent cookbook

**Platform:** Either (Cursor preferred when `CURSOR_API_TOKEN` set; else Claude CMA)  
**Security tier:** standard

## Handoff

- Orchestrates delivery chain; routes implementation to engineering agents
- Weekly planning spike: runs `sprint` plan → PR + labelled issues
- Maps to crew orchestration layer in headless mode (future `crews/` convergence)
