# frontend-engineer — Managed Agent cookbook

**Platform:** Cursor Cloud Agents  
**Security tier:** standard — branch + PR only; never push to `main`

## Handoff

- Implements `squad:site|blog|recipes` issues linked to epic task rows
- Uses `website/.cursor/environment.json` multi-repo setup (website + carinyaparc)
- Pre-schedule sanity: manual Cloud Agent run on one `squad:site` issue → PR

## Review chain

Peer review → `senior-frontend-engineer`; final gate → `principal-frontend-engineer` (separate cookbooks when expanded post-spike).
